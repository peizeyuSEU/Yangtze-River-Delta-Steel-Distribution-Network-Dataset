# Methodology

Source records are kept separately from raw public structured records. Processing uses fixed units, explicit scenario assumptions, cached OSRM distances, and registered transformations. Final data are a research calibration, not enterprise operating data.
